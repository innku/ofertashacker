<div class="mainContent clear span-24 topSpacing">
  <% custom_title "Iniciar sesión" %>
  <div class="infoCompany span-4">
  	<p>Iniciar Sesi&oacute;n</p>
  </div>
  <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
    <div class="generalForm span-12 prepend-1">
      <h1><%= t('.email') %></h1>
      <%= f.text_field :email, :required=>'required' %>

      <h1><%= t('.password') %></h1>
      <%= f.password_field :password, :required=>'required', :type => "password", :minlength => "6"%></p>

      <% if devise_mapping.rememberable? -%>
        <div class='calltoAction login'>
			<%= f.submit "Ingresar" %>
		</div>
        <p class='remember_me'>
			<%= f.check_box :remember_me %> <%= f.label :remember_me, t('.remember_me') %>
		</p>
      <% end -%>
    </div>
  <% end %>
  <%= render :partial => 'companies/shared/links' %>
</div>
