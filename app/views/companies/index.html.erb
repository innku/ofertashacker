
<div class="content">
  <% custom_title "Empresas"%>
	<div class="companies_wrapper">
		<div class="companies_sidebar">
			<div class="sidebar_top">
				<p>¿Tu empresa trabaja con Ruby y no est&aacute; en la lista?</p>
				<p><span>Reg&iacute;strate ta!</span></p>
			</div>
			<div class="sidebar_bottom">
				<p>Mant&eacute;nte informado</p>
			</div>
			
		</div>


	  <% @companies.each do |company| %>
	    <div class="company_profile shadow">
	        <span>
	        	<h2><%= link_to truncate(company.title, :length =>  55), company_path(company) %></h2>
		        <% unless company.city.blank?%><%= "En #{company.city}" %><br /><%end%>
		        <% if company.twitter?%>
		          <%= link_to "@#{truncate(company.twitter, :length => 20)}", (company.formated_twitter), :target => :blank%><br />
		        <%end%>
		        <% if company.facebook?%>
		          <%= link_to "facebook.com/#{truncate(company.facebook, :length =>10)}", (company.formated_facebook), :target => :blank%> 
		          <br />
		        <%end%>
	        </span>
	        <%if company.logo?%>
	        <%=link_to (image_tag company.logo.url(:medium)), company%>
	        <%end%>

	      <div class="clear"></div>
	   </div>   
		<% end %>
	</div>
	
</div>
