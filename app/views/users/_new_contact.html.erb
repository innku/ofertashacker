<div id="registration">
  <h3>Registro</h3>
  <div class="overlay_form">
    <div class="registration_left">
      <div class="sign_in_box">
        <p>¿Ya tienes cuenta? <%= link_to "Entrar", "#", :id=>"login" %></p>
      </div>
      <%= form_for @user,:id =>"user_new", :validate => :true, :url => user_registration_path(:job => job) do |f| %>
        
        <%= f.label t('.name') + '*' %>
        <%= f.text_field :name %>

        <%= f.label t('.email') + '*' %>
        <%= f.text_field :email %>
        
        <%= f.label t('.password') + '*' %>
        <%= f.password_field :password %>

        <%= f.label t('.password_confirmation') + '*' %>
        <%= f.password_field :password_confirmation %>
        <p class="desc">Le enviaremos el correo a la empresa y guardaremos tus datos para ahorrarte este paso la próxima vez que apliques a cualquier otra oferta.</p>
        <div id="captcha">
           <%= f.label t('.captcha') %>
          <%= raw (recaptcha_tag :challenge)%>
          <% if params[:job]%>
            <%= f.label t('.missing_catcha'), :class=>"message captcha"%>
          <% end%>
        </div>
    </div>
    <div class="registration_right">
      <%= f.label t('.message') %>
      <%= f.text_area :message %>

        <p><%= f.submit "Contactar", :class => "button"%></p>
    <% end %>
    </div>
  </div>
</div>

<div id="sign_in">
  <h3>Iniciar sesión</h3>
  <% if params[:sign_in] %>
    <%= content_tag "p", "Contraseña o email inválido", :class => :message %>
  <% end %>
  <%= form_for @user, :url => user_session_path(:job => job,:sign_in => true) do |f| %>

    <%= f.label :email %>
    <%= f.text_field :email %>
    <%=hidden_field_tag :job_id, params[:id].to_i%>

    <%= f.label t('.password') %>
    <%= f.password_field :password %>
    <p class="option">¿No tienes cuenta? <%=link_to "Regístrate", "#", :id=> "register"%></p>
    <p><%= f.submit "Entrar", :class=>"button" %></p>
  <% end %>
</div>


